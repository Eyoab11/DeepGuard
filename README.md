# DeepGuard: CICIDS2017 Preprocessing Pipeline

This repository contains a Jupyter Notebook (`DeepGuard_binary_CICIDS.ipynb`) for preprocessing the CICIDS2017 dataset for intrusion detection, integrated with Snort IDS.

## Setup
1. **Clone the repository**:
   ```bash
   git clone https://github.com/Eyoab11/deepguard.git
   cd deepguard
